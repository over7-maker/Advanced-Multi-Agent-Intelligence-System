# Version: v1.0.0
# Generated by AMAS Release System
name: Guaranteed Auto Response

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  guaranteed-response:
    runs-on: ubuntu-latest
    if: github.event.issue.state == 'open'
    
    permissions:
      issues: write
      pull-requests: write
      contents: read
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install requests
    
    - name: Guaranteed Auto Response
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}
        ISSUE_TITLE: ${{ github.event.issue.title }}
        ISSUE_BODY: ${{ github.event.issue.body }}
        ISSUE_AUTHOR: ${{ github.event.issue.user.login }}
      run: |
        python .github/scripts/simple_working_responder.py
    
    - name: Show Success Message
      run: |
        echo "🎉 Auto-response system is working!"
        echo "✅ Issue #${{ github.event.issue.number }} has been processed"
        echo "🤖 AI response posted successfully"