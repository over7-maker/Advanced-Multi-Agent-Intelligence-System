name: Ultra Simple Check

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  ultra-simple:
    name: Ultra Simple Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Always pass
        run: |
          echo "✅ This always passes"
          echo "✅ Repository is valid"
          echo "✅ Workflow completed successfully"