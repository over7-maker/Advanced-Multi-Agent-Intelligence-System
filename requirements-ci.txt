# CI/CD Dependencies for Data Governance & Compliance Module
# These are the exact versions used in CI/CD pipeline for reproducible builds

# Type Checking
mypy>=1.7.0
pyright>=1.1.330
# Note: types-all removed to avoid conflicts with project-specific type stubs
# Install specific type stubs as needed: types-requests, types-python-dateutil, etc.

# Linting
flake8>=6.1.0
pylint>=3.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-benchmark>=4.0.0

# Security
bandit>=1.7.5
safety>=2.3.5
