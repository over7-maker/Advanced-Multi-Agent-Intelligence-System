# CI/CD Dependencies for Data Governance & Compliance Module
# These are the exact versions used in CI/CD pipeline for reproducible builds
# All versions are pinned for security and reproducibility

# =============================================================================
# Type Checking Tools
# =============================================================================
mypy==1.18.2              # Static type checker for Python
pyright==1.1.407          # Microsoft's static type checker
typing-extensions==4.12.2  # Backported type hints for older Python versions
mypy-extensions==1.0.0    # Extensions to the standard typing module
pathspec==0.12.1          # Utility library for pattern matching of file paths

# =============================================================================
# Code Formatting & Linting Tools
# =============================================================================
black==24.4.2             # Opinionated code formatter
flake8==7.3.0             # Style guide enforcement (PEP 8)
pylint==4.0.2             # Code analysis for errors and code quality

# =============================================================================
# Testing Framework & Plugins
# =============================================================================
pytest==9.0.0                    # Testing framework
pytest-cov==7.0.0                # Coverage plugin for pytest
pytest-benchmark==4.0.0          # Benchmark plugin for pytest
pytest-rerunfailures==14.0       # Plugin for re-running flaky tests
coverage==7.6.1                  # Code coverage measurement
iniconfig==2.0.0                 # INI config parser for pytest
packaging==24.1                  # Core utilities for Python packages
pluggy==1.5.0                    # Plugin management for pytest

# =============================================================================
# Security Scanning Tools
# =============================================================================
bandit==1.7.10            # Security linter for Python code
safety==3.2.7             # Dependency vulnerability scanner

# Required dependencies for security tools
stevedore==5.3.0          # Manage dynamic plugins for Python apps (required by bandit)
pbr==6.1.0                # Python Build Reasonableness (required by stevedore)
GitPython==3.1.43         # Git interface (required by bandit)
PyYAML==6.0.2             # YAML parser (required by bandit)

# =============================================================================
# Supporting Libraries
# =============================================================================
# These are required by the above tools but may not be auto-installed
setuptools==75.6.0        # Package installation and distribution
wheel==0.45.1             # Built-package format for Python
pip==24.3.1               # Package installer

# =============================================================================
# Notes:
# - All versions are pinned for reproducibility and security
# - Update these versions when security patches are released
# - Check https://pypi.org/ for latest secure versions
# - Run 'pip install --upgrade -r requirements-ci.txt' to update all
# =============================================================================
