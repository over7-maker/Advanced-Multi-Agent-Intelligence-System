# AMAS - Advanced Multi-Agent Intelligence System
# Production-ready requirements with exact versions

# Core Web Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# AI and ML Core (Phase 1 - Core only)
openai==1.58.1
anthropic==0.40.0
google-generativeai==0.8.3
groq==0.13.0
# cohere==5.5.3  # Temporarily disabled due to dependency conflicts
# huggingface-hub==0.24.6  # Temporarily disabled due to dependency conflicts
# transformers==4.45.0  # Temporarily disabled due to dependency conflicts
# torch==2.5.1  # Temporarily disabled due to dependency conflicts
scikit-learn==1.6.0
numpy==2.2.1
pandas==2.2.3

# Database and Storage
sqlalchemy==2.0.36
alembic==1.14.0
redis==5.2.0
aioredis==2.0.1
neo4j==5.28.0

# Monitoring and Performance (Phase 1 - Core only)
# psutil==5.9.8  # Temporarily disabled due to dependency conflicts
# matplotlib==3.10.0  # Temporarily disabled due to dependency conflicts
# seaborn==0.13.2  # Temporarily disabled due to dependency conflicts
# plotly==5.24.1  # Temporarily disabled due to dependency conflicts

# Security and Authentication (Phase 1 - Core only)
bcrypt==4.2.1
pyjwt==2.10.1
cryptography==44.0.0
# bandit==1.7.10  # Temporarily disabled due to dependency conflicts
# safety==3.2.7  # Temporarily disabled due to dependency conflicts

# HTTP and Networking
httpx==0.28.1
aiohttp==3.11.11
requests==2.32.3

# Data Processing
networkx==3.4.2
joblib==1.4.2
python-multipart==0.0.20

# Development and Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
flake8==7.1.1
isort==5.13.2
mypy==1.13.0

# Utilities
python-dotenv==1.0.1
click==8.1.7
rich==13.9.4
tqdm==4.67.1
colorama==0.4.6

# Web Scraping (for OSINT)
beautifulsoup4==4.12.3
selenium==4.28.0

# Vector Search and Embeddings (Phase 1 - Core only)
# sentence-transformers==3.3.1  # Temporarily disabled due to dependency conflicts
# faiss-cpu==1.12.0  # Temporarily disabled due to dependency conflicts

# Additional Dependencies
pyyaml==6.0.2
