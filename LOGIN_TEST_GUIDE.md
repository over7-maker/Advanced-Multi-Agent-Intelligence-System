# 🔐 دليل تجربة تسجيل الدخول - Login Test Guide

## ✅ معلومات تسجيل الدخول

### بيانات الدخول الافتراضية:
- **Username**: `admin`
- **Password**: `admin123`

أو يمكنك استخدام:
- **Username**: `user`  
- **Password**: `user123` (إذا تم تعيين `AMAS_USER_PASSWORD`)

## 🌐 الوصول إلى صفحة تسجيل الدخول

1. **افتح المتصفح** وانتقل إلى:
   ```
   http://localhost:8000/login
   ```
   أو
   ```
   http://localhost:8001/login
   ```
   (حسب المنفذ الذي يعمل عليه الخادم)

2. **ستظهر صفحة تسجيل الدخول** مع:
   - حقل Username
   - حقل Password
   - زر Login

## 📝 خطوات تسجيل الدخول

1. **أدخل اسم المستخدم**: `admin`
2. **أدخل كلمة المرور**: `admin123`
3. **اضغط على زر "Login"**

## ✅ ما يجب أن يحدث بعد تسجيل الدخول

1. ✅ **يتم إرسال الطلب** إلى `/api/v1/login`
2. ✅ **يتم التحقق من البيانات** في Backend
3. ✅ **يتم إنشاء JWT Token** وإرجاعه
4. ✅ **يتم حفظ Token** في `localStorage`
5. ✅ **يتم الاتصال بـ WebSocket** تلقائياً
6. ✅ **يتم إعادة التوجيه** إلى `/dashboard`

## 🔧 إذا واجهت مشاكل

### مشكلة: Rate Limit Exceeded (429)
**الحل**: تم إصلاحها - تم إضافة `/api/v1/login` إلى قائمة المسارات المستثناة من Rate Limiting

### مشكلة: Invalid credentials (401)
**الحل**: تأكد من استخدام:
- Username: `admin`
- Password: `admin123`

### مشكلة: Frontend لا يتصل بالـ Backend
**الحل**: تأكد من:
1. الخادم يعمل على `http://localhost:8000` أو `http://localhost:8001`
2. Frontend يستخدم `baseURL: '/api/v1'` (نسبي)
3. لا توجد أخطاء CORS في Console

## 🧪 اختبار API مباشرة

يمكنك اختبار API مباشرة باستخدام curl:

```bash
curl -X POST http://localhost:8000/api/v1/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'
```

**الاستجابة المتوقعة**:
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "token_type": "bearer",
  "expires_in": 1800
}
```

## 📊 حالة النظام

- ✅ **Frontend Login Component**: جاهز
- ✅ **Backend Login Endpoint**: جاهز (`/api/v1/login`)
- ✅ **Authentication Manager**: جاهز
- ✅ **Rate Limiting**: تم إصلاحه (Login مستثنى)
- ✅ **WebSocket Connection**: يتم الاتصال تلقائياً بعد Login
- ✅ **Token Storage**: يتم حفظه في localStorage

## 🎯 الخطوات التالية بعد تسجيل الدخول

بعد تسجيل الدخول بنجاح، يمكنك:
1. **عرض Dashboard** - `/dashboard`
2. **إدارة Tasks** - إنشاء ومتابعة المهام
3. **إدارة Agents** - عرض وإدارة الوكلاء
4. **إدارة Integrations** - إعداد التكاملات
5. **متابعة System Health** - عرض حالة النظام

---

**ملاحظة**: إذا واجهت أي مشاكل، تحقق من:
- Console في المتصفح (F12)
- Network Tab لرؤية الطلبات
- Server Logs في Terminal

